const workspace = 'KaderMovies';
const screenIds = {
  WELCOME_SCREEN: `${workspace}.WelcomeScreen`,
  HOME_SCREEN: `${workspace}.HomeScreen`,
  MOVIES_SCREEN: `${workspace}.MovieScreen`,

};

export default screenIds;